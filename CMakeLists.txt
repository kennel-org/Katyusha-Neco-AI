cmake_minimum_required(VERSION 3.5)

# Enable testing
enable_testing()

# Include ESP-IDF project.cmake
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Define the project
project(katyusha_neco_ai)
